version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm ci --legacy-peer-deps

  pre_build:
    commands:
      - npm test -- --watch=false || true

  build:
    commands:
      - npm run build

artifacts:
  files:
    - '**/*'
  base-directory: build
